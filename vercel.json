{
  "version": 2,
  "installCommand": "npm install --production --legacy-peer-deps --no-audit --no-fund --loglevel error",
  "buildCommand": "mkdir -p public && echo 'Wiki.js Binary Deployment Successful. Note: This app requires a persistent server to run the wiki engine.' > public/index.html",
  "outputDirectory": "public"
}
